import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  innerWrapper: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
  },
  title: {
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 24,
    lineHeight: 28,
  },
});
